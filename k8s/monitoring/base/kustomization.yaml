apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - rbac.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app: monitoring
      part-of: gitops-poc
    includeSelectors: false
